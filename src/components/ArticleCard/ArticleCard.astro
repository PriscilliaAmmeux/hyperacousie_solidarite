---
import Button from "../../ui/Button/Button.astro";
import Title from "../../ui/Title/Title.astro";
import '../../styles/variables.css';
import './ArticleCard.css';

const { title, subtitle, image, link } = Astro.props;
---

<div class="article-card">
  {image && image.trim() !== "" && (
    <div class="article-card-img-wrapper">
      <img
        src={image}
        alt={`Illustration pour l'article ${title}`}
        class="article-card-img"
        loading="lazy"
      />
    </div>
  )}
  <div class="article-card-content">
    <Title
      text={title}
      className="article-card-title"
      level="4"
    />
    {subtitle && <p class="article-card-subtitle">{subtitle}</p>}
    <Button
      class="hide-article-btn"
      variant="teal"
      size="sm"
      href={link}
      target="_blank"
      rel="noopener"
      aria-label={`Lire l'article ${title} - ouvre une nouvelle page`}
    >
      Lire l’article
    </Button>
    <slot />
  </div>
</div>